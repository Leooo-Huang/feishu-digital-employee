# 飞书「收集数字员工」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给现有飞书知识库机器人加一个可移植的"收集数字员工"skill：上传文档/表格 + @机器人下指令 → 它在群里/私信主动对话收集信息、清洗确认、幂等写回文件、自动催办并收工汇报。

**Architecture:** 四层可移植设计——🧠 大脑是符合 agentskills.io 标准的 `feishu-collector` skill（SKILL.md 指令 + Node 辅助脚本）；✋ 手脚是官方 lark-cli skills（im/base/sheets/doc/contact/drive/event）；💾 记忆是飞书多维表格（任务表+槽位表）；💓 心跳是各运行时薄适配（event consume + 调度器）。skill 每次调用无状态，全部状态读写 Base，因此跨运行时且抗重启。详见 [设计文档](../specs/2026-06-16-飞书收集数字员工-design.md)。

**Tech Stack:** Node.js (ESM, ≥v20) · 内置 `node:test` 做测试（无额外依赖、无构建步骤）· `node:child_process execFile` 调 lark-cli · 飞书多维表格做状态库 · LLM（宿主运行时模型，CC 下为 Claude）做大脑推理。

**目录结构（全部自包含在一个可移植 skill 文件夹内）：**

```
skills/feishu-collector/
  SKILL.md                  # 大脑：完整工作流指令（LLM 读它执行）
  package.json              # ESM + test 脚本，零运行时依赖
  src/
    larkcli.js              # 薄封装：execFile 调 lark-cli，返回 stdout/JSON
    hash.js                 # contentHash() + needsWrite() 幂等决策（纯函数）
    state.js                # 任务/槽位状态机：常量 + nextSlotState()（纯函数）
    clean.js                # 归一化清洗器：身份证/尺码/日期/邮箱（纯函数）
    schedule.js             # selectSlotsToNudge() 催办挑选（纯函数）
    locator.js              # buildWriteCommand() 落点→lark-cli 写命令（纯函数）
    base-io.js              # 任务/槽位 CRUD + upsert（调 lark-cli base）
    contact.js              # resolvePerson() 人名→候选（调 lark-cli contact）
    file-parse.js           # parseSource()/localToOnline()（调 lark-cli sheets/base/doc/drive）
    messaging.js            # sendGroup()/sendDM()/buzz()（调 lark-cli im）
  bin/
    setup-base.js           # 一次性：在飞书建任务表+槽位表
    on-message.js           # 心跳入口①：处理一条 incoming 消息事件
    tick.js                 # 心跳入口②：定时催办/收工检查
  test/
    hash.test.js state.test.js clean.test.js schedule.test.js locator.test.js
    base-io.test.js         # 集成（沙箱 Base，需 TEST_BASE_TOKEN）
    e2e.test.js             # 端到端（沙箱群+Base+模拟账号）
docs/pipeline/env-capabilities.yaml   # 环境/命令面探测结果（Task 1 产出）
```

---

## 实施进度（2026-06-16 无监督时段，无外发副作用部分已完成）

> 已完成并提交：Task 1（命令面探测→`docs/pipeline/env-capabilities.yaml`）；Phase 1 全部确定性脊柱（hash/state/clean/schedule/locator，**30 单测全通过**）；`larkcli.js`（跨平台封装，`--version` 冒烟通过）；手脚层 base-io/contact/file-parse/messaging（用**实测命令名**编写）；SKILL.md 大脑；bin/setup-base、on-message（离线冒烟 OK）、tick（dry-run OK）。全部 `node --check` 通过。
>
> **命令名真相源**：下方各 Task 代码块里的 lark-cli 命令名是初稿（部分为示意），**以 `env-capabilities.yaml` 与已提交的 `src/` 实现为准**（实测：子命令带 `+` 前缀；`base +record-upsert`、`sheets +cells-set`、`contact +search-user --as user`、`im +messages-send`、`im messages urgent_app`、`docs +update`、`drive +import`+`+task_result`）。
>
> **未做（需用户在场，有外发副作用/需授权）**：Task 8 真正建飞书表、Task 11 发真人消息、Phase 2/4 集成与 e2e、补发送 scope/bot 入群。见 [设计 §6](../specs/2026-06-16-飞书收集数字员工-design.md) 与本文末"需用户参与的前置"。

---

## Phase 0 — 脚手架与环境探测

### Task 1: 探测 lark-cli 命令面与 scope，落盘 env-capabilities.yaml

**Files:**
- Create: `docs/pipeline/env-capabilities.yaml`
- Create: `skills/feishu-collector/package.json`

- [ ] **Step 1: 探测运行时与 lark-cli 版本/域命令面**

Run（逐条执行并记录真实输出，不要凭记忆）：
```bash
node --version
lark-cli --version
lark-cli base --help
lark-cli sheets --help
lark-cli contact --help
lark-cli im --help
lark-cli doc --help
lark-cli drive --help
lark-cli event list | grep -i message
```

- [ ] **Step 2: 探测当前已授权 scope**

Run：
```bash
lark-cli auth status 2>&1 || lark-cli config file
```
记录：`base:app`、`im:message`、`im:chat`、`contact:user`、`docx`/`sheets`、`drive`、`offline_access` 哪些已授权、哪些缺。

- [ ] **Step 3: 把探测结果写入 `docs/pipeline/env-capabilities.yaml`**

格式（用真实输出填充，禁止猜测；缺失项标 `available: false` 并附探测命令）：
```yaml
# 探测于 2026-06-16，所有 available 字段必须有对应命令实际输出为证
node: { version: "<实测>", available: true }
lark_cli:
  version: "<实测>"
  commands:
    base:    { subcommands: ["record-create","record-update","record-search","table-create", ...], verified_help: true }
    sheets:  { subcommands: ["..."], verified_help: true }
    contact: { subcommands: ["user-search","user-batch-get", ...], verified_help: true }
    im:      { subcommands: ["message-create","message-urgent-app", ...], verified_help: true }
    doc:     { subcommands: ["..."], verified_help: true }
    drive:   { subcommands: ["import-task", ...], verified_help: true }
scopes:
  granted: ["<实测已授权列表>"]
  missing: ["<实测缺失列表>"]
  sensitive_not_needed: ["im:message.group_msg"]   # 本功能不需要
```

- [ ] **Step 4: 建 Node 项目骨架**

`skills/feishu-collector/package.json`：
```json
{
  "name": "feishu-collector",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/",
    "test:unit": "node --test test/hash.test.js test/state.test.js test/clean.test.js test/schedule.test.js test/locator.test.js",
    "test:int": "node --test test/base-io.test.js",
    "test:e2e": "node --test test/e2e.test.js"
  }
}
```

- [ ] **Step 5: 验证骨架可运行**

Run: `cd skills/feishu-collector && node --test test/ 2>&1 || echo "no tests yet (expected)"`
Expected: 提示无测试文件（正常，尚未创建）。

- [ ] **Step 6: Commit**

```bash
git add docs/pipeline/env-capabilities.yaml skills/feishu-collector/package.json
git commit -m "chore: probe lark-cli command surface + scaffold feishu-collector skill"
```

---

## Phase 1 — 确定性脊柱（纯函数，零飞书副作用，全单测）

> 这是质量分水岭：幂等、状态机、清洗、催办挑选、落点命令都必须可单测。先把这层做扎实。

### Task 2: 幂等核心 `hash.js`

**Files:**
- Create: `skills/feishu-collector/src/hash.js`
- Test: `skills/feishu-collector/test/hash.test.js`

- [ ] **Step 1: Write the failing test**

```javascript
// test/hash.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { contentHash, needsWrite } from '../src/hash.js';

test('contentHash 对相同归一化输入稳定', () => {
  assert.equal(contentHash(' L '), contentHash('L'));
});
test('contentHash 对不同输入不同', () => {
  assert.notEqual(contentHash('L'), contentHash('M'));
});
test('needsWrite: 空旧指纹必写', () => {
  assert.equal(needsWrite('', 'L'), true);
});
test('needsWrite: 同值 skip', () => {
  const h = contentHash('L');
  assert.equal(needsWrite(h, 'L'), false);
});
test('needsWrite: 变值覆盖', () => {
  const h = contentHash('L');
  assert.equal(needsWrite(h, 'M'), true);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/hash.test.js`
Expected: FAIL（`Cannot find module '../src/hash.js'`）

- [ ] **Step 3: Write minimal implementation**

```javascript
// src/hash.js
import { createHash } from 'node:crypto';

/** 归一化：trim + 折叠内部空白 + 小写，保证语义相同的值指纹一致 */
export function normalizeForHash(value) {
  return String(value ?? '').trim().replace(/\s+/g, ' ').toLowerCase();
}

/** 内容指纹：归一化后取 sha256 前 16 hex */
export function contentHash(value) {
  return createHash('sha256').update(normalizeForHash(value)).digest('hex').slice(0, 16);
}

/** 幂等决策：旧指纹为空（从未写）或与新值指纹不同 → 需要写 */
export function needsWrite(oldHash, newValue) {
  if (!oldHash) return true;
  return oldHash !== contentHash(newValue);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/hash.test.js`
Expected: PASS（5 tests）

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/src/hash.js skills/feishu-collector/test/hash.test.js
git commit -m "feat: idempotent contentHash + needsWrite"
```

### Task 3: 状态机 `state.js`

**Files:**
- Create: `skills/feishu-collector/src/state.js`
- Test: `skills/feishu-collector/test/state.test.js`

- [ ] **Step 1: Write the failing test**

```javascript
// test/state.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { SLOT_STATES, TASK_STATES, nextSlotState } from '../src/state.js';

test('合法迁移：已问+收到回答 → 收到原始', () => {
  assert.equal(nextSlotState('已问', 'answer_received'), '收到原始');
});
test('清洗不通过 → 留在清洗中（追问）', () => {
  assert.equal(nextSlotState('清洗中', 'clean_failed'), '清洗中');
});
test('清洗通过 → 待确认', () => {
  assert.equal(nextSlotState('清洗中', 'clean_ok'), '待确认');
});
test('确认通过 → 已填', () => {
  assert.equal(nextSlotState('待确认', 'confirmed'), '已填');
});
test('确认被否 → 回清洗中', () => {
  assert.equal(nextSlotState('待确认', 'rejected'), '清洗中');
});
test('到期未填 → 跳过', () => {
  assert.equal(nextSlotState('已问', 'deadline_passed'), '跳过');
});
test('非法迁移抛错', () => {
  assert.throws(() => nextSlotState('已填', 'answer_received'));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/state.test.js`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```javascript
// src/state.js
export const SLOT_STATES = ['待问','已问','收到原始','清洗中','待确认','已填','跳过','不适用','待澄清'];
export const TASK_STATES = ['草稿','待确认','收集中','暂停','已完成','已取消'];

// 槽位迁移表：当前态 -> { 事件: 下一态 }
const SLOT_TRANSITIONS = {
  '待问':    { ask_sent: '已问', deadline_passed: '跳过', mark_na: '不适用', need_clarify: '待澄清' },
  '已问':    { answer_received: '收到原始', deadline_passed: '跳过', mark_na: '不适用' },
  '收到原始': { start_clean: '清洗中' },
  '清洗中':  { clean_failed: '清洗中', clean_ok: '待确认', deadline_passed: '跳过' },
  '待确认':  { confirmed: '已填', rejected: '清洗中', deadline_passed: '跳过' },
  '待澄清':  { resolved: '待问', mark_na: '不适用' },
  '已填':    { reopen: '清洗中' },   // 改答
  '跳过':    { reopen: '待问' },     // 迟交补收
};

/** 给定当前态 + 事件，返回下一态；非法组合抛错 */
export function nextSlotState(current, event) {
  const row = SLOT_TRANSITIONS[current];
  if (!row || !(event in row)) {
    throw new Error(`非法槽位状态迁移: ${current} --${event}-->`);
  }
  return row[event];
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/state.test.js`
Expected: PASS（7 tests）

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/src/state.js skills/feishu-collector/test/state.test.js
git commit -m "feat: slot/task state machine with guarded transitions"
```

### Task 4: 清洗归一化器 `clean.js`

**Files:**
- Create: `skills/feishu-collector/src/clean.js`
- Test: `skills/feishu-collector/test/clean.test.js`

- [ ] **Step 1: Write the failing test**

```javascript
// test/clean.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { cleanIdCard, cleanSize, cleanEmail, cleanDate } from '../src/clean.js';

test('身份证：合法 18 位通过', () => {
  assert.deepEqual(cleanIdCard('11010119900307123X'),
    { ok: true, value: '11010119900307123X' });
});
test('身份证：含空格归一化后通过', () => {
  assert.equal(cleanIdCard(' 110101 1990 0307 1234 ').ok, true);
});
test('身份证：位数不足返回追问', () => {
  const r = cleanIdCard('12345');
  assert.equal(r.ok, false);
  assert.match(r.followup, /18 位/);
});
test('尺码：全角/大小写归一化', () => {
  assert.deepEqual(cleanSize('ｌ'), { ok: true, value: 'L' });
  assert.deepEqual(cleanSize(' xl '), { ok: true, value: 'XL' });
});
test('尺码：非法值追问', () => {
  assert.equal(cleanSize('大号').ok, false);
});
test('邮箱：合法通过、非法追问', () => {
  assert.equal(cleanEmail('a@b.com').ok, true);
  assert.equal(cleanEmail('a@@b').ok, false);
});
test('日期：多格式归一到 YYYY-MM-DD', () => {
  assert.equal(cleanDate('2026/6/9').value, '2026-06-09');
  assert.equal(cleanDate('2026年6月9日').value, '2026-06-09');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/clean.test.js`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```javascript
// src/clean.js
const toHalfWidth = (s) => String(s ?? '').replace(/[！-～]/g,
  (c) => String.fromCharCode(c.charCodeAt(0) - 0xFEE0)).replace(/　/g, ' ');

export function cleanIdCard(raw) {
  const v = toHalfWidth(raw).replace(/\s/g, '').toUpperCase();
  if (/^\d{17}[\dX]$/.test(v)) return { ok: true, value: v };
  return { ok: false, followup: '这个身份证号看起来不对（应为 18 位），能再发一次完整号码吗？' };
}

export function cleanSize(raw) {
  const v = toHalfWidth(raw).trim().toUpperCase();
  const allow = ['XS','S','M','L','XL','XXL','XXXL'];
  if (allow.includes(v)) return { ok: true, value: v };
  return { ok: false, followup: `尺码请从 ${allow.join('/')} 里选一个，你要哪个？` };
}

export function cleanEmail(raw) {
  const v = toHalfWidth(raw).trim();
  if (/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)) return { ok: true, value: v };
  return { ok: false, followup: '邮箱格式好像不对，能再发一次吗？' };
}

export function cleanDate(raw) {
  const v = toHalfWidth(raw).trim();
  const m = v.match(/(\d{4})\D+(\d{1,2})\D+(\d{1,2})/);
  if (!m) return { ok: false, followup: '日期没看懂，麻烦按 年-月-日 发我一下？' };
  const [, y, mo, d] = m;
  return { ok: true, value: `${y}-${mo.padStart(2,'0')}-${d.padStart(2,'0')}` };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/clean.test.js`
Expected: PASS（7 tests）

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/src/clean.js skills/feishu-collector/test/clean.test.js
git commit -m "feat: normalizing cleaners (idcard/size/email/date)"
```

### Task 5: 催办挑选 `schedule.js`

**Files:**
- Create: `skills/feishu-collector/src/schedule.js`
- Test: `skills/feishu-collector/test/schedule.test.js`

- [ ] **Step 1: Write the failing test**

```javascript
// test/schedule.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { selectSlotsToNudge } from '../src/schedule.js';

const base = { deadline: '2026-06-19T18:00:00+08:00', reminderIntervalH: 24, maxAttempts: 3 };
const now = new Date('2026-06-17T18:00:00+08:00');

test('未填且超过催办间隔 → 入选', () => {
  const slots = [{ slot_id:'s1', 状态:'已问', 最近询问时间:'2026-06-16T10:00:00+08:00', 追问次数:0 }];
  const r = selectSlotsToNudge(slots, base, now);
  assert.equal(r.length, 1);
});
test('已填 → 不催', () => {
  const slots = [{ slot_id:'s1', 状态:'已填', 最近询问时间:'2026-06-16T10:00:00+08:00', 追问次数:0 }];
  assert.equal(selectSlotsToNudge(slots, base, now).length, 0);
});
test('刚问过（未到间隔）→ 不催', () => {
  const slots = [{ slot_id:'s1', 状态:'已问', 最近询问时间:'2026-06-17T12:00:00+08:00', 追问次数:0 }];
  assert.equal(selectSlotsToNudge(slots, base, now).length, 0);
});
test('超过最大追问次数 → 不再催（标记 escalate）', () => {
  const slots = [{ slot_id:'s1', 状态:'已问', 最近询问时间:'2026-06-15T10:00:00+08:00', 追问次数:3 }];
  assert.equal(selectSlotsToNudge(slots, base, now).length, 0);
});
test('临近截止（<6h）→ urgent=true', () => {
  const nearNow = new Date('2026-06-19T14:00:00+08:00');
  const slots = [{ slot_id:'s1', 状态:'已问', 最近询问时间:'2026-06-18T10:00:00+08:00', 追问次数:1 }];
  const r = selectSlotsToNudge(slots, base, nearNow);
  assert.equal(r[0].urgent, true);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/schedule.test.js`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```javascript
// src/schedule.js
const H = 3600 * 1000;
const OPEN = new Set(['待问','已问','收到原始','清洗中','待确认','待澄清']); // 未终结=未填

/** 返回需要催办的槽位（附 urgent 标记）。纯函数：now 由调用方注入，便于测试 */
export function selectSlotsToNudge(slots, task, now) {
  const deadline = new Date(task.deadline);
  const hoursToDeadline = (deadline - now) / H;
  if (hoursToDeadline < 0) return []; // 已过期由 tick 走收工逻辑，不在此催
  const urgent = hoursToDeadline <= 6;
  return slots.filter((s) => {
    if (!OPEN.has(s.状态)) return false;
    if ((s.追问次数 ?? 0) >= task.maxAttempts) return false;
    const last = s.最近询问时间 ? new Date(s.最近询问时间) : new Date(0);
    return (now - last) / H >= task.reminderIntervalH;
  }).map((s) => ({ ...s, urgent }));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/schedule.test.js`
Expected: PASS（5 tests）

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/src/schedule.js skills/feishu-collector/test/schedule.test.js
git commit -m "feat: nudge selection with deadline/interval/maxAttempts + urgency"
```

### Task 6: 落点写命令生成 `locator.js`

**Files:**
- Create: `skills/feishu-collector/src/locator.js`
- Test: `skills/feishu-collector/test/locator.test.js`

> 依据 Task 1 记录的 `lark-cli base/sheets/doc` 实测子命令填充命令模板；下方 argv 形态以官方命令面为准，若 Task 1 实测命令名不同，按实测改这里的常量（不是占位，是以实测为唯一真相源）。

- [ ] **Step 1: Write the failing test**

```javascript
// test/locator.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildWriteCommand } from '../src/locator.js';

test('sheet 落点 → sheets 写单元格 argv', () => {
  const argv = buildWriteCommand(
    { kind:'sheet', token:'shtcnX', sheetId:'0b', cell:'C5' }, 'L');
  assert.equal(argv[0], 'sheets');
  assert.ok(argv.includes('shtcnX'));
  assert.ok(argv.includes('C5'));
  assert.ok(argv.includes('L'));
});
test('base 落点 → base record-update argv', () => {
  const argv = buildWriteCommand(
    { kind:'base', appToken:'bascnX', tableId:'tblX', recordId:'recX', field:'尺码' }, 'L');
  assert.equal(argv[0], 'base');
  assert.ok(argv.includes('recX'));
});
test('未知 kind 抛错', () => {
  assert.throws(() => buildWriteCommand({ kind:'pdf' }, 'x'));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/locator.test.js`
Expected: FAIL（模块不存在）

- [ ] **Step 3: Write minimal implementation**

```javascript
// src/locator.js
// 注意：argv 子命令名/旗标必须与 Task 1 落盘的 env-capabilities.yaml 中实测一致。
// 下方为按官方命令面写的形态；实施时对照 `lark-cli base/sheets/doc --help` 校正常量。
export function buildWriteCommand(loc, value) {
  switch (loc.kind) {
    case 'sheet':
      return ['sheets', 'cell-update',
        '--spreadsheet-token', loc.token,
        '--sheet-id', loc.sheetId,
        '--range', loc.cell,
        '--value', String(value)];
    case 'base':
      return ['base', 'record-update',
        '--app-token', loc.appToken,
        '--table-id', loc.tableId,
        '--record-id', loc.recordId,
        '--fields', JSON.stringify({ [loc.field]: value })];
    case 'doc':
      return ['doc', 'block-update',
        '--doc', loc.token,
        '--block-id', loc.blockId,
        '--text', String(value)];
    default:
      throw new Error(`不支持的落点类型: ${loc.kind}`);
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/locator.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/src/locator.js skills/feishu-collector/test/locator.test.js
git commit -m "feat: write-back command builder per locator kind"
```

---

## Phase 2 — lark-cli 手脚封装（集成测试用沙箱）

> 这层把脊柱接到真实飞书。集成测试需要一个**测试用多维表格**（`TEST_BASE_TOKEN` 环境变量）和测试群，**绝不碰真实业务数据/真人**。

### Task 7: lark-cli 薄封装 `larkcli.js`

**Files:**
- Create: `skills/feishu-collector/src/larkcli.js`
- Test: `skills/feishu-collector/test/base-io.test.js`（本任务先建文件，下个任务补用例）

- [ ] **Step 1: 实现 execFile 封装**

```javascript
// src/larkcli.js
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
const pexec = promisify(execFile);

const BIN = process.env.LARK_CLI_BIN || 'lark-cli';

/** 调 lark-cli，返回 { stdout, stderr }；非零退出抛错并带 stderr */
export async function lark(argv, { profile } = {}) {
  const args = profile ? ['--profile', profile, ...argv] : argv;
  try {
    const { stdout, stderr } = await pexec(BIN, args, { maxBuffer: 16 * 1024 * 1024 });
    return { stdout, stderr };
  } catch (e) {
    throw new Error(`lark-cli ${args.join(' ')} 失败: ${e.stderr || e.message}`);
  }
}

/** 调 lark-cli 并把 stdout 解析为 JSON（要求命令带 --json/输出 JSON） */
export async function larkJson(argv, opts) {
  const { stdout } = await lark(argv, opts);
  return JSON.parse(stdout);
}
```

- [ ] **Step 2: 冒烟验证（不依赖授权）**

Run:
```bash
cd skills/feishu-collector
node -e "import('./src/larkcli.js').then(m=>m.lark(['--version'])).then(r=>console.log('OK',r.stdout.trim()))"
```
Expected: 打印 `OK lark-cli version <x>`

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/src/larkcli.js
git commit -m "feat: thin lark-cli exec wrapper (lark/larkJson)"
```

### Task 8: Base 状态库 I/O `base-io.js` + 建表脚本

**Files:**
- Create: `skills/feishu-collector/src/base-io.js`
- Create: `skills/feishu-collector/bin/setup-base.js`
- Test: `skills/feishu-collector/test/base-io.test.js`

- [ ] **Step 1: 写建表脚本 `bin/setup-base.js`**

依据 [设计 §2](../specs/2026-06-16-飞书收集数字员工-design.md) 的任务表/槽位表字段，调 `lark-cli base table-create`（命令名以 Task 1 实测为准）建两张表，打印 `appToken/tableId`。脚本顶部注释列出所建字段清单（与设计 §2 完全一致）。

- [ ] **Step 2: Write the failing integration test**

```javascript
// test/base-io.test.js
import { test, before } from 'node:test';
import assert from 'node:assert/strict';
import { createTask, createSlots, querySlotsByStatus, upsertSlotValue } from '../src/base-io.js';

const RUN = process.env.TEST_BASE_TOKEN ? test : test.skip; // 无沙箱 token 则跳过

RUN('创建任务→建槽位→按状态查→upsert 写值→再查指纹', async () => {
  const ctx = { appToken: process.env.TEST_BASE_TOKEN };
  const taskId = await createTask(ctx, { 标题:'e2e测试', 状态:'收集中', 场所:'私信',
    截止时间:'2026-06-30T18:00:00+08:00', 催办策略:'24h', maxAttempts:3 });
  assert.ok(taskId);
  await createSlots(ctx, taskId, [
    { 字段名:'尺码', 对象:'张三', 责任人原文:'张三', 状态:'待问' }]);
  const pending = await querySlotsByStatus(ctx, taskId, '待问');
  assert.equal(pending.length, 1);
  const slotId = pending[0].slot_id;
  const r1 = await upsertSlotValue(ctx, slotId, 'L');
  assert.equal(r1.written, true);
  const r2 = await upsertSlotValue(ctx, slotId, 'L'); // 同值
  assert.equal(r2.written, false); // 幂等 skip
});
```

- [ ] **Step 3: Run to verify it fails (or skips without token)**

Run: `node --test test/base-io.test.js`
Expected: 有 `TEST_BASE_TOKEN` 时 FAIL（模块未实现）；无则 SKIP。

- [ ] **Step 4: 实现 `base-io.js`**

实现 `createTask/createSlots/querySlotsByStatus/upsertSlotValue/getSlot/updateSlotState`，全部通过 `larkJson(['base', ...])`。`upsertSlotValue` 内部用 `hash.needsWrite()` 比对 `内容指纹` 字段，决定是否真正写、并回写新指纹；返回 `{ written: boolean }`。字段名与设计 §2 一一对应。

- [ ] **Step 5: 先建沙箱表再跑测试**

Run:
```bash
node bin/setup-base.js   # 打印 TEST_BASE_TOKEN
export TEST_BASE_TOKEN=<上一步输出>
node --test test/base-io.test.js
```
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add skills/feishu-collector/src/base-io.js skills/feishu-collector/bin/setup-base.js skills/feishu-collector/test/base-io.test.js
git commit -m "feat: Base task/slot store with idempotent upsert + setup script"
```

### Task 9: 人名解析 `contact.js`

**Files:**
- Create: `skills/feishu-collector/src/contact.js`
- Test: `skills/feishu-collector/test/base-io.test.js`（追加用例，沙箱可选）

- [ ] **Step 1: 实现 `resolvePerson`**

```javascript
// src/contact.js
import { larkJson } from './larkcli.js';

/**
 * 把人名/邮箱/@提及解析成飞书用户候选。
 * @returns {Promise<{status:'unique'|'ambiguous'|'none', candidates:Array<{open_id,name}>}>}
 *   unique→直接用；ambiguous→交 LLM/发起人消歧；none→标 待澄清
 */
export async function resolvePerson(raw, { scopeChatId } = {}) {
  const argv = ['contact', 'user-search', '--query', raw, '--json'];
  if (scopeChatId) argv.push('--chat-id', scopeChatId); // 优先群成员范围匹配
  const res = await larkJson(argv);
  const candidates = (res.items || res.users || []).map(u => ({ open_id: u.open_id, name: u.name }));
  if (candidates.length === 1) return { status: 'unique', candidates };
  if (candidates.length === 0) return { status: 'none', candidates };
  return { status: 'ambiguous', candidates };
}
```

- [ ] **Step 2: 集成验证（沙箱可选）**

Run（有授权时用一个真实同事名验证，否则跳过）：
```bash
node -e "import('./src/contact.js').then(m=>m.resolvePerson(process.argv[1])).then(console.log)" "黄文轩"
```
Expected: 打印 `{ status, candidates }`

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/src/contact.js
git commit -m "feat: person name resolution via lark-cli contact (unique/ambiguous/none)"
```

### Task 10: 文件解析与本地转换 `file-parse.js`

**Files:**
- Create: `skills/feishu-collector/src/file-parse.js`

- [ ] **Step 1: 实现 `parseSource` 与 `localToOnline`**

- `parseSource(ref)`：按 ref 类型（sheet/base/doc 的 token，或飞书 URL）调对应 `lark-cli sheets/base/doc` 读出结构（表头、行、责任人列候选、可填列），返回标准化结构 `{ kind, token, headers, rows, fillableColumns, assigneeColumn? }`。
- `localToOnline(localPath)`：本地 xlsx/docx → 调 `lark-cli drive import-task`（命令名以 Task 1 实测为准）转成飞书在线文档，返回新 token。**转换失败必须抛错并附原因，禁止静默 fallback**（遵守全局规则）。

- [ ] **Step 2: 冒烟验证**

Run（用一个测试在线表格 token）：
```bash
node -e "import('./src/file-parse.js').then(m=>m.parseSource({kind:'sheet',token:process.argv[1]})).then(r=>console.log(JSON.stringify(r,null,2)))" <test-sheet-token>
```
Expected: 打印标准化结构（headers/rows/fillableColumns）

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/src/file-parse.js
git commit -m "feat: source file parsing + local-to-online conversion (no silent fallback)"
```

### Task 11: 消息收发 `messaging.js`

**Files:**
- Create: `skills/feishu-collector/src/messaging.js`

- [ ] **Step 1: 实现 `sendGroup/sendDM/buzz`**

```javascript
// src/messaging.js
import { lark } from './larkcli.js';

export async function sendGroup(chatId, text) {
  await lark(['im', 'message-create', '--receive-id-type', 'chat_id',
    '--receive-id', chatId, '--msg-type', 'text',
    '--content', JSON.stringify({ text })]);
}
export async function sendDM(openId, text) {
  await lark(['im', 'message-create', '--receive-id-type', 'open_id',
    '--receive-id', openId, '--msg-type', 'text',
    '--content', JSON.stringify({ text })]);
}
/** 飞书加急（buzz）；命令名以 Task 1 实测为准。节制使用：仅 urgent 时调 */
export async function buzz(messageId, openIds) {
  await lark(['im', 'message-urgent-app', '--message-id', messageId,
    '--user-id-list', openIds.join(','), '--user-id-type', 'open_id']);
}
```

- [ ] **Step 2: 冒烟验证（发给自己）**

Run（用自己的 open_id，避免打扰他人）：
```bash
node -e "import('./src/messaging.js').then(m=>m.sendDM(process.argv[1],'collector 自测消息')).then(()=>console.log('sent'))" ou_8bf451b1232a8295d7043c7dc3f3d36c
```
Expected: 打印 `sent`，自己飞书收到消息。

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/src/messaging.js
git commit -m "feat: messaging helpers (group/DM/buzz) via lark-cli im"
```

---

## Phase 3 — 大脑（SKILL.md）与心跳入口

### Task 12: 写 `SKILL.md`（工作流指令）

**Files:**
- Create: `skills/feishu-collector/SKILL.md`

- [ ] **Step 1: 写 frontmatter + 工作流指令**

frontmatter（agentskills.io 标准）：
```markdown
---
name: feishu-collector
description: 飞书"收集数字员工"。当用户上传文档/表格并 @机器人 要求收集其中信息、或查询收集进度、或暂停/取消收集任务时使用。在群里/私信主动对话收集、清洗确认、幂等写回文件、自动催办与收工汇报。
---
```

正文按设计 §3 写成 LLM 可执行的分支指令，至少覆盖：
1. **意图分类**：收到 @机器人 消息 → 判断 新发起 / 进度查询 / 暂停取消 / 开放式登记上报。
2. **发起流程**：调 `file-parse.parseSource`（本地文件先 `localToOnline`）→ 用 LLM 把结构+指令转成收集计划（字段、责任人原文、场所、截止、催办策略）→ 对每个责任人原文调 `contact.resolvePerson`（none/ambiguous 走消歧）→ 在群里发**发起确认**卡片 → 用户确认后 `base-io.createTask/createSlots` 落库 → 发首轮询问 `messaging.sendDM/sendGroup` + `updateSlotState(ask_sent)`。
3. **收集流程**：收到收集对象回复 → `base-io` 载入此人 open 槽位 → LLM 映射答案到槽位 → 调 `clean.*` 清洗（失败→追问、留 `清洗中`）→ 复述确认（否→回 `清洗中`）→ `base-io.upsertSlotValue` 写回 + `updateSlotState(confirmed)`。
4. **进度查询**：聚合槽位状态 → 回"已收 X/Y，差 …"。
5. **收工**：全部已填或到期 → 群里汇报 + 附文件链接 → 任务置 `已完成`。

明确写入两条铁律：① **写真人前先发起确认**；② **写回前复述确认**。并列出"绝不编造缺失值、403 不静默失败、本地转换失败不静默 fallback"。

- [ ] **Step 2: 用 lark-skill-maker 校验格式**

Run: 用 `lark-skill-maker` skill 校验 frontmatter/结构合规（或对照其它 `lark-*` skill 的 SKILL.md 结构自检）。
Expected: 格式合规，无缺字段。

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/SKILL.md
git commit -m "feat: SKILL.md collection workflow brain (agentskills.io format)"
```

### Task 13: 心跳入口 `on-message.js` 与 `tick.js`

**Files:**
- Create: `skills/feishu-collector/bin/on-message.js`
- Create: `skills/feishu-collector/bin/tick.js`

- [ ] **Step 1: 写 `on-message.js`**

读取一条 incoming 事件（从 stdin JSON 或 argv），判断是否与收集相关（@机器人 或 来自有 open 槽位的收集对象）；相关则把"当前任务/槽位快照 + 这条消息"组织好，交给宿主 LLM 按 SKILL.md 决策（CC 下即当前 agent；OpenClaw 下由其 agent 循环）。无状态：只读 Base、产出动作、写回 Base。

- [ ] **Step 2: 写 `tick.js`**

无参定时入口：遍历 `状态=收集中` 的任务 → `schedule.selectSlotsToNudge` → 对入选槽位发提醒（`urgent` 则 `messaging.buzz`）、`追问次数+1`、更新 `最近询问时间`；过期任务走收工汇报。

- [ ] **Step 3: 冒烟验证 tick（空跑）**

Run: `node bin/tick.js --dry-run`
Expected: 打印"扫描到 N 个收集中任务，本轮需催办 M 个槽位（dry-run 未发送）"

- [ ] **Step 4: 写各运行时挂载说明（写入 SKILL.md 末尾"部署"小节）**

- **Claude Code**：用 `/schedule` 或 `lark-cli event consume im.message.receive_v1` 管道到 `on-message.js`；`tick.js` 用 `/schedule` 周期触发。
- **OpenClaw**：装飞书通道插件把消息路由到 agent；`openclaw cron` 注册 `tick.js`（自带 cron）。
- **Hermes**：gateway 接飞书；`tick.js` 外接系统 cron。

- [ ] **Step 5: Commit**

```bash
git add skills/feishu-collector/bin/on-message.js skills/feishu-collector/bin/tick.js skills/feishu-collector/SKILL.md
git commit -m "feat: heartbeat entries (on-message + tick) and per-runtime mount docs"
```

---

## Phase 4 — 端到端沙箱验收

### Task 14: 三形态端到端闭环（沙箱）

**Files:**
- Test: `skills/feishu-collector/test/e2e.test.js`

- [ ] **Step 1: 写 e2e 测试（需 `TEST_BASE_TOKEN` + `TEST_CHAT_ID` + 模拟账号）**

三条用例，各跑"发起→确认→收集→清洗→确认→写回→收工"：
1. 表格按行收集（每人填自己行）
2. 问题清单向特定人
3. 开放式登记（追加新行）
断言：最终目标文件单元格/行被正确填入、槽位全 `已填`、重复 upsert 不重写。

- [ ] **Step 2: 跑 e2e**

Run: `TEST_BASE_TOKEN=... TEST_CHAT_ID=... node --test test/e2e.test.js`
Expected: PASS（3 用例）。**仅在测试群/测试 Base、用模拟账号，不碰真人。**

- [ ] **Step 3: Commit**

```bash
git add skills/feishu-collector/test/e2e.test.js
git commit -m "test: e2e sandbox coverage for all three collection shapes"
```

### Task 15: 抗重启 + 跨运行时可移植验证

- [ ] **Step 1: 抗重启**

收集中途 `kill` 进程 → 重启 `on-message.js` 重放同一条消息 → 断言：不重复发问、不重复写（幂等）、状态从 Base 正确恢复。

- [ ] **Step 2: 可移植**

同一 `skills/feishu-collector/` 分别在 Claude Code 与 OpenClaw 下各跑一遍沙箱发起→收集片段，确认行为一致（脚本与 Base 不变，仅心跳层不同）。

- [ ] **Step 3: 全量回归 + Commit**

Run: `cd skills/feishu-collector && npm test`
Expected: 单元全 PASS；集成/e2e 在有沙箱 token 时 PASS。
```bash
git add -A && git commit -m "test: restart-safety + cross-runtime portability verification"
```

---

## 自查（plan vs spec）

- **覆盖**：§1 架构→全 Phase；§2 数据模型→Task 8 建表/I/O + Task 2 幂等；§3 流程/状态机→Task 3 状态机 + Task 12 SKILL.md + Task 13 心跳；§4 边界→Task 4 清洗(11)、Task 9 人名(1/2)、Task 5 催办(7)、locator/base-io 403 与不覆盖(4/6)、file-parse 不静默 fallback(12)；§5 测试→Phase 1 单元 + Task 8/14 集成 + Task 15 抗重启/可移植。无未覆盖项。
- **占位符扫描**：无 TBD/TODO；lark-cli 子命令名以 Task 1 实测为唯一真相源（已显式标注，非占位）。
- **类型一致**：`upsertSlotValue→{written}`、`resolvePerson→{status,candidates}`、`selectSlotsToNudge→slots[]+urgent`、`nextSlotState(current,event)`、`buildWriteCommand(loc,value)→argv` 在各任务引用一致。

## 需用户参与的前置（见设计 §6"等你醒来"清单）

选用哪个应用/租户、补授缺失 scope、首次真人测试、选 OpenClaw 挂常驻心跳——这些有外发副作用或需拍板，留待用户在场执行。Phase 0-1（脚手架+脊柱单测）与 Phase 2-4 的代码编写无外发副作用，可先行完成。
