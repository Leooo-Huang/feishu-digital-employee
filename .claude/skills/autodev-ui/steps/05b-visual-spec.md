# 步骤 5b：视觉规范（L3）

**目标**：定义项目的视觉设计语言，确保实现阶段不用"猜"配色、字体、间距。

## 决策来源

1. 读取 `*-design.md` 中的技术选型——确认使用的 UI 框架（shadcn/MUI/Ant/自定义）
2. 如果用户提供了品牌指南或偏好，以用户输入为准
3. 如果没有，基于产品调性自动推荐：
   - 科技/效率类产品 → 冷色（蓝/紫）+ 直角/小圆角 + 紧凑密度
   - 消费/生活类产品 → 暖色（橙/绿）+ 大圆角 + 舒适密度
   - 企业/B2B 产品 → 中性（蓝灰）+ 小圆角 + 紧凑密度

## 5 个核心决策

### 1. 品牌色

定义色彩系统，产出格式：

| 角色 | 色值 | CSS 变量 | 用途 |
|------|------|---------|------|
| 主色 | {hex} | `--primary` | 按钮、链接、焦点环、选中态 |
| 主色悬停 | {hex} | `--primary-hover` | 按钮悬停态 |
| 辅助色 | {hex} | `--accent` | 标签、徽章、强调元素 |
| 成功 | {hex} | `--success` | 成功状态、完成标记、正向操作 |
| 警告 | {hex} | `--warning` | 警告提示、需注意的状态 |
| 错误 | {hex} | `--destructive` | 错误状态、删除操作、危险操作 |
| 背景 | {hex} | `--background` | 页面背景 |
| 卡片背景 | {hex} | `--card` | 卡片、面板背景 |
| 边框 | {hex} | `--border` | 分割线、输入框边框 |
| 文字-主要 | {hex} | `--foreground` | 标题、正文 |
| 文字-次要 | {hex} | `--muted-foreground` | 辅助文字、占位符 |

如果 UI 框架是 shadcn，直接给出 HSL 值以匹配其 CSS 变量命名。

### 2. 排版

| 层级 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| Display | 36px | Bold (700) | 1.2 | 营销页大标题 |
| H1 | 30px | Bold (700) | 1.3 | 页面标题 |
| H2 | 24px | Semibold (600) | 1.35 | 区域标题 |
| H3 | 20px | Semibold (600) | 1.4 | 卡片标题 |
| H4 | 16px | Medium (500) | 1.45 | 小节标题 |
| Body | 14px | Regular (400) | 1.6 | 正文 |
| Small | 13px | Regular (400) | 1.5 | 辅助文字 |
| Caption | 12px | Regular (400) | 1.4 | 时间戳、标签 |

字体族：{选择}（如 `Inter, system-ui, sans-serif`）

### 3. 圆角风格

| 元素 | 圆角 | 说明 |
|------|------|------|
| 按钮 | {值} | |
| 卡片 | {值} | |
| 输入框 | {值} | |
| 弹窗/对话框 | {值} | |
| 头像 | full (50%) | 圆形 |
| 标签/徽章 | full | 药丸形 |

整体风格：{直角(0px) / 小圆角(4-6px) / 中圆角(8-12px) / 大圆角(16px+)}

### 4. 间距与信息密度

基础单位：{4px}
间距刻度：`{4 / 8 / 12 / 16 / 24 / 32 / 48 / 64}px`

| 场景 | 间距 | 说明 |
|------|------|------|
| 组件内边距 | {值} | 按钮、卡片内部 |
| 组件间距 | {值} | 同级元素之间 |
| 区域间距 | {值} | 页面大区块之间 |
| 页面边距 | {值} | 内容区与屏幕边缘 |

信息密度：{紧凑 / 舒适 / 宽松}

### 5. 阴影与层级

| 层级 | 阴影值 | 用途 |
|------|--------|------|
| 无 | none | 扁平元素 |
| 低 | `0 1px 2px rgba(0,0,0,0.05)` | 卡片默认 |
| 中 | `0 4px 6px rgba(0,0,0,0.07)` | 悬浮卡片、下拉菜单 |
| 高 | `0 10px 15px rgba(0,0,0,0.1)` | 模态框、弹窗 |

### 6. 图标系统（必选）

<IMPORTANT>
这是独立的必选决策，不是可选项。**禁止使用 emoji 作为 UI 图标**。

原因：emoji 跨平台渲染不一致（Windows/macOS/Android/iOS 形态差异大）、无法自定义尺寸和颜色、深浅主题切换时对比度失控、无法 tree-shake、让界面看起来业余。
</IMPORTANT>

#### 必填字段

| 字段 | 选项 | 说明 |
|------|------|------|
| 图标库 | Lucide / Heroicons / Phosphor / Radix Icons / Tabler / Remix Icon / 自定义 SVG | **必填，不能留空** |
| 图标风格 | 线性（stroke）/ 填充（fill）/ 双色（duotone） | 整个项目保持统一，避免混用 |
| 默认尺寸 | 12 / 14 / 16 / 20 / 24 / 32 px | 对应按钮内、标签前、导航、功能按钮、装饰图标 |
| 描边粗细（线性风格） | 1 / 1.5 / 2 px | 与字重协调 |
| 图标颜色 | `currentColor`（继承文字色）/ 指定 token | 默认用 `currentColor` 跟随主题 |

#### 推荐选择规则

| 项目类型 | 推荐 icon 库 | 理由 |
|----------|------------|------|
| shadcn/ui 项目 | **Lucide**（默认） | shadcn 官方配套，API 风格一致 |
| Tailwind 项目 | Heroicons | Tailwind 团队维护，outline/solid 双版本 |
| 需要丰富图标（>1000） | Phosphor | 6 种风格、图标数最多 |
| 品牌感强 / 极简 | Radix Icons | 极简几何风，适合工具型产品 |
| 国内用户为主 | Remix Icon / Tabler | 国内 CDN 可用，无墙风险 |

与 Phase 2 `*-oss-scan.md` 对齐：若扫描清单已给出推荐 icon 库，优先选清单里的方案；选型后在 `*-design.md` 技术选型表和本 `*-ui.md` 视觉规范章节**同时记录**。

#### emoji 的允许场景（白名单）

仅以下场景允许 emoji，其他一律禁用：

1. **i18n 文案 JSON 的内容值**（用户可见文案，不是 UI 元素），且 emoji 有明确语义
2. **代码注释、commit message、README 等文档**
3. **用户生成内容**（UGC）的展示（来自用户输入，不是我们写的）
4. **品牌明确要求的标志**（如产品 logo 本身包含 emoji 字符）

禁止场景（全部必须改用 icon 库）：

- 按钮图标（删除、编辑、设置、关闭等）
- 导航图标（侧栏、tab 栏、面包屑）
- 状态指示（成功 / 失败 / 警告 / 加载 / 空状态）
- 装饰图标（空状态插画、引导卡片、feature 列表的勾选）
- **任何直接写在 JSX / Vue template / Svelte template 的 text 节点中的 emoji**

## 额外决策（可选）

- 暗色模式：{支持 / 不支持 / 后续考虑}
- 插图风格：{无 / 简约线条 / 3D / 手绘}

## 规则

- 如果 UI 框架有默认主题（如 shadcn 的 default/new-york），优先用默认值，只覆盖品牌色
- 色值必须同时提供亮色和暗色版本（如果支持暗色模式）
- 所有数值用 CSS 变量表达，不硬编码到组件中

--- 步骤 5b 完成 ---
