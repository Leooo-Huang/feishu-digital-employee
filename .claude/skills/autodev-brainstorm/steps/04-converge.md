# 步骤 4：CONVERGE — 收敛

**判断当前模式：**

读取 `docs/pipeline/config.yaml`，检查 `gates` 中是否有阶段 2 的门控。

**自动模式（无门控）：**
- 选择总分最高的方案
- 平分时按简洁性决胜
- 简洁性也平分时选择工作量最小的
- 展示："自动选择方案 N：{名称}（总分 {X}，最高分）"

**门控模式（有门控）：**
- 展示评分对比表
- 标注推荐方案及推荐理由
- 等待用户选择
- 如果用户不满意任何方案 → 回到步骤 2 重新发散
